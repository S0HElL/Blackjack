Blackjack cardgame webapp using flask.

You're gonna need to install the flask library in your directory.

Open the bash terminal and use ```pip install -r requirements.txt``` to be able to run the code. 
